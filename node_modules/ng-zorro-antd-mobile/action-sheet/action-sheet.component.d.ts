import { TemplateRef, OnInit, OnDestroy } from '@angular/core';
import { Subject } from 'rxjs';
import { LocaleProviderService } from '../locale-provider/locale-provider.service';
export declare class ActionSheetComponent implements OnInit, OnDestroy {
    private localeProviderService;
    unsubscribe$: Subject<void>;
    option: any;
    constructor(localeProviderService: LocaleProviderService);
    ngOnInit(): void;
    localeProvider(): void;
    onPress(index: any, rowIndex: number, event: any): void;
    showShare(option: any): {
        [x: string]: boolean;
    };
    setActiveClassName(option: any, suffix: any): string[];
    isNoTitle(value: string | TemplateRef<any>): boolean;
    isTemplateRef(value: any): boolean;
    isArray(options: any, value: any): boolean;
    ngOnDestroy(): void;
}
